import React from 'react'

export default function PostTitle(props) {

    /**
     * Renders React Elements
     * @returns 
     */
    return (
        <h1>
            {props.title}
        </h1>
    )
}
